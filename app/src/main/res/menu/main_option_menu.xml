<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:title="Main Menu"
        app:showAsAction="always"
        android:icon="@drawable/ic_more">
        <menu>
            <item
                android:id="@+id/options_light_theme"
                android:title="Day Theme"
                android:icon="@drawable/ic_light"
                />
            <item
                android:id="@+id/options_dark_theme"
                android:title="Night Theme"
                android:icon="@drawable/ic_dark"
                />
            <item
                android:id="@+id/options_about"
                android:title="@string/option_about"
                android:icon="@drawable/ic_info"
                />
        </menu>
    </item>
</menu>